# AgroPulse Mobile (Flutter Application)

This is the mobile frontend for AgroPulse tailored for farmers and buyers.

## Running the Mobile App

1. Ensure Flutter SDK is installed.
2. Run dependencies installation:
   ```bash
   flutter pub get
   ```
3. Launch on Android Emulator or physical device:
   ```bash
   flutter run
   ```
